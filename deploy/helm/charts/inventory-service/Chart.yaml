apiVersion: v2
name: inventory-service
description: Inventory saga participant — atomic stock reservation, TTL release, cache.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: platform-lib
    version: 0.1.0
    repository: file://../platform-lib
