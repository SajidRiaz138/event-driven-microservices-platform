apiVersion: v2
name: payment-service
description: Payment saga participant — authorize, capture, refund, reconcile.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: platform-lib
    version: 0.1.0
    repository: file://../platform-lib
