apiVersion: v2
name: order-service
description: Saga orchestrator — owns the order aggregate and drives the async saga.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: platform-lib
    version: 0.1.0
    repository: file://../platform-lib
